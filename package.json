{
  "name": "agent-os-test-skills",
  "version": "1.0.0",
  "description": "Test project to verify Agent OS subagents use Skills",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
